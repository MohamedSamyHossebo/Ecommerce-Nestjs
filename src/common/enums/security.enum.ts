export const securityEnum = {
  BCRYPT: 'bcrypt',
  ARGON2: 'argon',
};

export const TOKEN_TYPE_ENUM = {
  ACCESS: 0,
  REFRESH: 1,
};

export const SIGNATURE_ENUM = {
  ADMIN: 'admin',
  USER: 'user',
};
